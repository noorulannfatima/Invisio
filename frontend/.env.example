# Copy to .env:
#   cp .env.example .env

# Backend API base URL (include /api; must match backend PORT)
VITE_API_URL=http://localhost:3000/api

# true = browse all pages with mock data (no Postgres/backend)
VITE_DEMO_MODE=false
